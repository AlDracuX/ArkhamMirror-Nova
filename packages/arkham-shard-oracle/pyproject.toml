[project]
name = "arkham-shard-oracle"
version = "0.1.0"
description = "Legal research and authority search assistant"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "arkham-frame>=0.1.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "black", "mypy"]

[project.entry-points."arkham.shards"]
oracle = "arkham_shard_oracle:OracleShard"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
