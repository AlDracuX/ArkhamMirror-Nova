[project]
name = "arkham-shard-skeleton"
version = "0.1.0"
description = "Legal argument and submission builder"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "arkham-frame>=0.1.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "black", "mypy"]

[project.entry-points."arkham.shards"]
skeleton = "arkham_shard_skeleton:SkeletonShard"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
