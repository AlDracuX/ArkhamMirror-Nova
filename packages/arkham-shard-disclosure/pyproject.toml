[project]
name = "arkham-shard-disclosure"
version = "0.1.0"
description = "Disclosure request and gap tracker"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "arkham-frame>=0.1.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "black", "mypy"]

[project.entry-points."arkham.shards"]
disclosure = "arkham_shard_disclosure:DisclosureShard"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
