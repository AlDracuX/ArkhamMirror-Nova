[project]
name = "arkham-shard-digest"
version = "0.1.0"
description = "ADHD-optimized daily briefings and case summaries"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "arkham-frame>=0.1.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "black", "mypy"]

[project.entry-points."arkham.shards"]
digest = "arkham_shard_digest:DigestShard"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
