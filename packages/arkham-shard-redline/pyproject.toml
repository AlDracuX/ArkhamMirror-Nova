[project]
name = "arkham-shard-redline"
version = "0.1.0"
description = "Document version comparison and semantic diff"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "arkham-frame>=0.1.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "black", "mypy"]

[project.entry-points."arkham.shards"]
redline = "arkham_shard_redline:RedlineShard"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
