[project]
name = "arkham-shard-sentiment"
version = "0.1.0"
description = "Tone and language pattern analyzer"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "arkham-frame>=0.1.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "black", "mypy"]

[project.entry-points."arkham.shards"]
sentiment = "arkham_shard_sentiment:SentimentShard"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
