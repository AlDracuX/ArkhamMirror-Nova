[project]
name = "arkham-shard-chain"
version = "0.1.0"
description = "Cryptographic evidence chain of custody"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "arkham-frame>=0.1.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "black", "mypy"]

[project.entry-points."arkham.shards"]
chain = "arkham_shard_chain:ChainShard"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
