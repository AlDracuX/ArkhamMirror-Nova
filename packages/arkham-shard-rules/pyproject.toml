[project]
name = "arkham-shard-rules"
version = "0.1.0"
description = "Procedural rules and deadline engine"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "arkham-frame>=0.1.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "black", "mypy"]

[project.entry-points."arkham.shards"]
rules = "arkham_shard_rules:RulesShard"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
