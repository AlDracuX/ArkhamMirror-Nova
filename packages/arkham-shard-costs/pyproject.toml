[project]
name = "arkham-shard-costs"
version = "0.1.0"
description = "Litigation costs and conduct tracker"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "arkham-frame>=0.1.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "black", "mypy"]

[project.entry-points."arkham.shards"]
costs = "arkham_shard_costs:CostsShard"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
